# Dependencies
node_modules/

# Bản build
dist/

# Môi trường & log
.env
.env.*
*.log
*.err

# Dữ liệu runtime — server tự tạo lại khi thiếu
server/data/orders.json

# Artifacts kiểm thử (ảnh chụp, dump) — script trong tools/ tự tạo lại khi chạy
tools/artifacts/*
!tools/artifacts/.gitkeep

# Hệ điều hành
.DS_Store
Thumbs.db


# Screenshot QA chụp ở root (cur-*.png)
/cur-*.png
